# Communication

- Responses are displayed in a terminal. Use terminal-friendly plain text.
- Do not use LaTeX commands, braces, or display-math delimiters when explaining
  equations to the user.
- Write equations in readable ASCII, and put code implementations in fenced
  code blocks.
